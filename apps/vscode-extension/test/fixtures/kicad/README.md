# KiCad Fixture Corpus

This directory is generated by `scripts/generate-kicad-fixture-corpus.mjs` for OASLANA-36.
Do not edit fixture files by hand; update the generator and run:

```bash
corepack pnpm run fixtures:kicad:generate
```

Validate integrity with:

```bash
corepack pnpm run test:fixtures
```
